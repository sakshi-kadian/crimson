defaults:
  - model: resnet50
  - dataset: cifar100
  - hardware: single_gpu
  - _self_

# Global settings
project_name: "crimson"
output_dir: "./outputs"
seed: 42

# Logging backend: "tensorboard" or "wandb"
logger: "tensorboard"
